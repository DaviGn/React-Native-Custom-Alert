import CustomAlert from './customAlert';
import { CustomAlertProvider, useCustomAlert } from './customAlertContext';

export { CustomAlertProvider as CustomAlertContext, useCustomAlert };
export default CustomAlert;
